import { createTheme } from "@mui/material";

export const theme = createTheme({
  typography: {
    fontFamily: "inter",
    h2: {
      fontWeight: "900",
      fontSize: "2rem",
      color: "#ead2ea",
    },
    body1: {
      fontWeight: "bold",
    },
  },
});
